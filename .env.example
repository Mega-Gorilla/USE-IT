# Optional environment overrides for USE-IT.
# Default settings now live in config.yaml—copy config.yaml.example first and only
# keep entries here for secrets you do not want to commit to disk.

# --- Logging ---------------------------------------------------------------
# Override the default logging level or log file destinations.
# BROWSER_USE_LOGGING_LEVEL=info
# BROWSER_USE_DEBUG_LOG_FILE=debug.log
# BROWSER_USE_INFO_LOG_FILE=info.log
# CDP_LOGGING_LEVEL=WARNING

# --- Telemetry -------------------------------------------------------------
# Disable anonymous telemetry if required.
# ANONYMIZED_TELEMETRY=false

# --- Cloud API -------------------------------------------------------------
# Browser Use Cloud credentials (optional).
# BROWSER_USE_API_KEY=your_api_key_here
# BROWSER_USE_CLOUD_API_URL=https://api.browser-use.com
# BROWSER_USE_CLOUD_SYNC=false

# --- Provider API Keys -----------------------------------------------------
# These override llm.api_keys.* entries from config.yaml when present.
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# AZURE_OPENAI_KEY=az-...
# AZURE_OPENAI_ENDPOINT=https://your-endpoint.openai.azure.com/
# GOOGLE_API_KEY=AIza...
# DEEPSEEK_API_KEY=...
# GROK_API_KEY=...
# NOVITA_API_KEY=...

# --- Browser Overrides -----------------------------------------------------
# Explicit browser paths or flags.
# BROWSER_USE_EXECUTABLE_PATH=/path/to/chrome
# BROWSER_USE_HEADLESS=true
# BROWSER_USE_USER_DATA_DIR=./browser_data
# PLAYWRIGHT_BROWSERS_PATH=.playwright-browsers

# --- Proxy Settings --------------------------------------------------------
# BROWSER_USE_PROXY_URL=http://proxy.example.com:8080
# BROWSER_USE_NO_PROXY=localhost,127.0.0.1,*.internal
# BROWSER_USE_PROXY_USERNAME=username
# BROWSER_USE_PROXY_PASSWORD=password
